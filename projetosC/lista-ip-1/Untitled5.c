#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main()
{
    int i, contador;
    
    scanf("%d", &i);
    contador = 0;
     for (i= 1; i <= 1000; i++){
     	if (i % 2 == 1){ contador = contador+1;
		 }
}
printf ("%d\n",contador);
return 0; 
}
